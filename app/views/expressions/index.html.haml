%h1 Listing expressions

%table.expressions
  %tr
    %th{:width=> 100} Image
    %th Name
    %th Definition
    %th
    %th

  - @expressions.each do |expression|
    %tr
      %td= link_to image_tag(expression.image(:small)), expression
      %td= expression.name
      %td= expression.definition.first(200)
      %td= link_to 'Edit', edit_expression_path(expression)
      %td= link_to 'Destroy', expression, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Expression', new_expression_path
